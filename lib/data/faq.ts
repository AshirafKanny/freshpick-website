export interface FaqItem {
  question: string;
  answer: string;
}

/**
 * Intentionally empty: no confirmed FreshPick FAQ content exists yet.
 * Populate once real, business-approved answers are available — never
 * fabricate policies, hours, or claims here.
 */
export const FAQ_ITEMS: FaqItem[] = [];
